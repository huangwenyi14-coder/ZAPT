# 报告驱动数据生成质量治理工作日志

## 目标

在 EvidenceForge 现有能力上实现 TXT 与可复制文字 PDF 的一键输入，经过 MiniMax 事实抽取、审核、受控补全和确定性场景编译，输出与原报告语义贴合且带来源审计的标准数据集。

## 关联资料

- 设计：`docs/superpowers/specs/2026-08-26-report-fidelity-quality-design.md`
- 原始输入输出设计：`docs/superpowers/specs/2026-08-26-report-to-dataset-design.md`
- 实施计划：`docs/superpowers/plans/2026-08-26-report-fidelity-quality-implementation-plan.md`
- 实施基线提交：`09512e1`

## 当前状态

- 设计：已确认。
- 实施计划：已确认，正在按工作包执行。
- 工作包 0：已完成并提交语义回归基线。
- 工作包 1：已完成并提交来源片段、事实账本和引用契约。
- 工作包 2：已完成并提交 MiniMax 事实抽取、审核与受控补全契约。
- 工作包 3：已完成并提交确定性场景编译器和显式补全请求。
- 工作包 4：已完成并提交原生注册表查询与来源标记。
- 工作包 5：已完成并提交 A/B/C 端到端管线、质量门与原子发布。
- 工作包 6：已完成并提交旧 APT 转换器危险兜底收紧。
- 工作包 7：已完成实施与离线回归，随本次提交归档。
- 下一步：工作包 8，使用真实 MiniMax 对 APT-C-48 PDF 做三次独立验收。

## 已知仓库基线

- `uv run ruff check .` 在本计划开始前存在 111 个与本任务无关的历史错误，主要位于 `defense_agent` 和旧脚本。
- `uv run ruff format --check .` 在本计划开始前报告 38 个历史文件需要格式化。
- 实施中不批量修复上述无关问题；每次提交要求本次修改的 Python 文件通过定向 Ruff 检查，并在最终阶段复核全仓差异。

## 迭代记录模板

每轮实施追加一节，不覆盖历史记录：

```text
### YYYY-MM-DD：工作包或根因名称

- 输入与运行范围：
- 本轮假设：
- 发现的根因：
- 通用修改：
- 新增或更新测试：
- 定向测试结果：
- 完整离线测试结果：
- 真实 MiniMax 调用次数：
- 来源贴合硬门槛：
- 来源贴合综合分：
- 未解决问题与下一步：
- 提交：
```

不得在工作日志中记录 API 密钥、完整报告正文、模型原始响应或用户未授权公开的运行数据。

### 2026-08-27：工作包 0——报告语义质量回归基线

- 输入与运行范围：只读取 APT-C-48 示例 PDF 的可复制文字层以核对关键事实；仓库内新增三份完全虚构且短小的 TXT 夹具。没有提交报告全文、截图或模型响应。
- 本轮假设：在改造生产链路前，先建立与内部模型解耦的语义快照和严格期望，才能持续识别事实丢失、虚构进程、跨行为 URL 串绑和读写语义颠倒。
- 发现的根因：第一版测试辅助函数直接复用了期望对象；向实际快照注入虚构 IOC 时会同时污染期望，导致潜在假通过。
- 通用修改：为 APT-C-48 固化 3 个 MD5、4 个 URL、域名/IP、两个任务、CMD、关键行为与禁止关系；为无 IOC、仅网络、多任务/多文件/多 URL 三类 TXT 建立独立期望；新增严格 Pydantic 夹具模型、深复制快照和通用语义断言。
- 新增或更新测试：新增 11 个离线集成自测，覆盖夹具完整性、项目 TXT 抽取器、正向匹配、虚构 PowerShell、CMD 错误归属、诱饵 URI 继承、注册表查询变写入和简短报告虚构 IOC。
- 定向测试结果：`uv run pytest --no-cov tests/integration/test_report_to_dataset_semantics.py`，11 个测试全部通过。
- 完整离线测试结果：`uv run pytest --no-cov tests/unit/test_report_ingest.py tests/integration/test_report_to_dataset_semantics.py`，现有报告导入测试与新基线共 38 个测试全部通过。
- 真实 MiniMax 调用次数：0。
- 来源贴合硬门槛：尚未接入生成链路，本轮只建立可执行期望。
- 来源贴合综合分：尚未计算。
- 未解决问题与下一步：当前快照由测试构造，尚未从真实场景和 Ground Truth 自动提取；工作包 1 先建立可引用的来源事实契约。
- 提交：`test: 固化报告语义质量回归基线`。

### 2026-08-27：工作包 1——来源片段、事实账本与引用契约

- 输入与运行范围：修改报告文本抽取、结构模型和 IOC 本地校验；只读运行 APT-C-48 示例 PDF 的文本层检查，没有调用 MiniMax，也没有生成或发布数据集。
- 本轮假设：先让“来源”成为可由本地代码验证的数据结构，再让 MiniMax 输出账本；否则继续优化提示词仍无法阻止错页引用、跨段取值或 AI 补全伪装成报告事实。
- 发现的根因：旧 `ExtractedDocument` 只有带页标记的整段字符串，没有稳定片段、字符偏移和摘要；旧 IOC 候选只保留首个规范值，没有出现位置；旧 `ReportExtraction` 将行为、实体和关系混在步骤参数中，无法验证悬空关系或规范化重复实体。
- 通用修改：新增带页码或 TXT 段落号、字符区间、SHA-256 和受限引用文本的 `SourceSpan`；新增 `source`、`derived`、`ai_completed`、`engine_generated` 字段来源；新增事实、实体、关系、报告级 IOC 和事实账本严格模型；增加 URL/哈希直接来源限制、实体规范化与确定性去重、关系和引用完整性校验；IOC 候选保留全部字符出现位置和所属片段。
- 新增或更新测试：新增 10 个来源契约测试，覆盖稳定 TXT 段落、PDF 页码、越界和错页、IOC 位置、URL 尾斜杠去重、多任务/多 URL/多哈希、来源/AI 边界、悬空关系以及精确值只能在所引片段内匹配。
- 定向测试结果：`uv run pytest --no-cov tests/unit/test_report_ingest.py tests/integration/test_report_to_dataset_semantics.py`，48 个测试全部通过；相关 Python 文件通过定向 Ruff 检查和格式检查。
- 完整离线测试结果：全仓默认套件共收集 5,118 项；运行 3 分 22 秒后在既有重型生成用例中安全中止，当时 105 项通过、5 项跳过、0 项失败。该结果只记为部分回归，不宣称全仓通过。
- 真实 PDF 检查：APT-C-48 PDF 提取为 8 页、3,581 个规范化字符和 10 个来源片段；10 个 IOC 候选全部能定位到来源片段。
- 真实 MiniMax 调用次数：0。
- 来源贴合硬门槛：引用结构和禁止补全契约已建立，尚未接入 MiniMax A/B/C 和最终质量门。
- 来源贴合综合分：尚未计算。
- 未解决问题与下一步：旧流水线仍使用单次 `ReportExtraction`；工作包 2 将让 MiniMax 客户端接受阶段化工具 Schema，并建立 A/B/C 三个版本化中文提示词与有限结构修复。
- 提交：`feat: 建立报告事实账本与来源引用契约`。

### 2026-08-27：工作包 2——MiniMax 事实抽取、审核与受控补全契约

- 输入与运行范围：修改报告事实模型、MiniMax 客户端、中文提示词、审核校验和报告导入测试；没有读取凭据文件、调用真实 MiniMax、生成数据集或改动旧场景模板。
- 本轮假设：仅把同一个大提示词多跑两遍不能根治质量问题；必须把模型职责拆成来源事实抽取 A、非改写式审核 B 和编译器指定字段补全 C，并为每个阶段提供不同的严格 Schema 和本地越权校验。
- 发现的根因：旧客户端硬编码 `ReportExtraction` 工具和工具名，无法让不同阶段使用不同输出契约；旧提示词把来源理解、场景拓扑、事件类型和 AI 补全混在一起；分块结果缺少事实级合并；审核没有“只提建议、不改账本”的数据结构；调用记录无法安全区分阶段和有限修复。
- 通用修改：新增 `ReportFactExtraction`、`FactAuditResult`、`CompletionRequest` 和 `SimulationPlan` 严格模型；新增分块事实、实体、关系和 IOC 合并；新增审核建议的来源片段、单动作载荷和精确实体值校验；新增补全请求白名单来源、禁止外部 IOC/哈希/命令字段及目标一致性校验；MiniMax 客户端支持动态工具名、动态 Schema、通用结构化解包和只含阶段、版本、耗时、令牌数的脱敏调用记录；新增 A/B/C 三套版本化中文提示词和每阶段最多一次的有限结构修复提示词。
- 新增或更新测试：新增 9 个阶段化测试，覆盖 A 不生成场景、B 不改写账本、C 不接收报告全文、有限修复、分块多任务/多 URL/多哈希不丢失、审核精确值引用、单建议载荷互斥、补全越权拒绝和 MiniMax 动态工具 Schema/脱敏调用记录。
- 定向测试结果：`uv run pytest --no-cov tests/unit/test_report_ingest.py tests/integration/test_report_to_dataset_semantics.py -q`，57 个测试全部通过；相关文件通过定向 Ruff 检查、格式检查和 `git diff --check`。
- 完整离线测试结果：本轮没有重复运行耗时较长的全仓 5,118 项套件；工作包 1 已记录当时的无失败部分回归，本轮执行报告导入与语义质量相关的完整定向集合。
- 真实 MiniMax 调用次数：0。
- 来源贴合硬门槛：A/B/C 的职责边界、来源类型和结构化越权校验已建立；旧 `ReportPipeline` 尚未切换到这些接口，因此当前命令行生成仍走旧 `ReportExtraction` 流程。
- 来源贴合综合分：尚未计算，待确定性编译器接入并生成真实结果后评估。
- 未解决问题与下一步：工作包 3 建立“事实账本 → 场景中间表示”的确定性编译器，由编译器产生显式 `CompletionRequest`；之后再把 A/B/C 接入端到端流水线，而不是直接用模型重写最终场景。
- 提交：`b1e3f2e feat: 拆分 MiniMax 抽取审核与补全流程`。

### 2026-08-27：工作包 3——报告专用确定性场景编译器

- 输入与运行范围：研究现有 typed storyline 事件、旧 APT JSON 转换器和场景模型；新增报告专用编译中间表示、确定性路由、来源属性校验和单元测试。没有读取示例 PDF、凭据文件或模型响应，没有调用 MiniMax，也没有生成数据集。
- 本轮假设：模型只负责把自然语言变成事实和关系；进程主体、网络行为、URI、HTTP 方法、计划任务、注册表读写和可见性必须由本地编译器根据直接关系决定，不能继续依赖“最后一个进程”、战役级网络字典或默认 PowerShell。
- 发现的根因：旧转换器包含最终 PowerShell、战役网络上下文和跨步骤进程兜底，不适合作为新报告导入黑盒；A 阶段行为词、关系词和实体 attributes 没有受控词表，确定性路由会因同义词漂移而降级；任务二进制、触发器、注册表值和服务属性虽然标为 `source`，此前没有本地核对其引用片段；提示词禁止 AI 猜测解释器，但本地补全校验尚未重复执行该禁令。
- 通用修改：新增带字段级来源、主体/客体/通道实体、预期可见性、物化状态、降级原因和合成必要性说明的编译中间表示；新增事实账本到 process、connection、dns_query、file_delete、scheduled_task_created、service_installed、registry_query、registry_set 和 email_message 的确定性路由；进程只接受直接 `performed_by`、来源命令首项推导或显式 C 阶段补全，稳定生成 `process_ref`；URL 的主机、端口、URI 和方法只从当前事实绑定的 URL 推导，域名/IP 仅按明确 `resolves_to` 映射；每个计划任务独立生成；无法无损物化的事实进入 `ground_truth_only`；为领域行为、关系谓词和任务/注册表/服务 attributes 增加 A 阶段中文约束；本地拒绝来源片段中不存在的关键 attributes，以及 AI 为未披露进程补出的 PowerShell、CMD、WScript、CScript 或网络地址。
- 新增或更新测试：新增 10 个编译器测试和 1 个来源属性测试，覆盖进程不跨事实继承、CMD 只拥有明确下载、URI/方法不跨网络行为、多计划任务独立、注册表读写区分、不可观测行为降级、字段来源与可见性、合成桥接禁带外部 IOC、受控进程补全以及本地拒绝虚构命令解释器。
- 定向测试结果：`uv run pytest --no-cov tests/unit/test_report_compiler.py tests/unit/test_report_ingest.py tests/integration/test_report_to_dataset_semantics.py -q`，68 个测试全部通过；相关文件通过定向 Ruff 检查、格式检查和 `git diff --check`。
- 完整离线测试结果：本轮没有重复运行全仓重型生成套件；工作包 1 已记录全仓部分回归，本轮完整运行所有报告编译、导入和语义质量相关测试。
- 真实 MiniMax 调用次数：0。
- 来源贴合硬门槛：编译器已禁止跨事实主体/URI 继承、无来源关键 attributes 和虚构解释器；当前产物是进入 `scenario.yaml` 前的严格中间表示，尚未由主流水线使用。
- 来源贴合综合分：尚未计算，待工作包 4/5 接通原生事件、Ground Truth 和生成前后质量门后评估。
- 未解决问题与下一步：`registry_query` 目前只存在于报告编译中间表示，工作包 4 要把它加入场景 Pydantic 模型、规范事件、Storyline 执行和 eCAR `REGISTRY/READ`，并让来源元数据进入 Ground Truth；报告命令仍走旧流水线，计划在质量门工作包统一切换。
- 提交：`16cc248 feat: 新增报告专用场景编译器`。

### 2026-08-27：工作包 4——原生注册表读取与 Ground Truth 来源披露

- 输入与运行范围：修改场景模型、故事线执行器、规范事件、eCAR、语义/记录级 Ground Truth、场景参考和覆盖文档；没有读取示例 PDF、凭据文件或模型响应，没有调用 MiniMax，没有生成正式数据集。
- 本轮假设：当原报告说“读取注册表”时，事实语义优先于日志数量；如果某数据源没有该读取证据，应明确不可见，不能伪造成写入。
- 发现的根因：原项目只有 `registry_set` 场景事件和 `registry_modify` 规范事件，无法对报告中的注册表查询进行无损物化；场景属性与 Ground Truth 没有事件/字段级来源容器，AI 补全与原文事实无法在最终产物中区分。
- 通用修改：新增 `registry_query` 严格场景模型，使用现有 `process_ref` 绑定读取主体；故事线执行为 `registry_read`，eCAR 输出 `REGISTRY/READ`，Sysmon 仍不处理读取；记录级可检测性只将 eCAR 判为直接证据；为所有 typed event 新增可选 `source_metadata`，包含事实编号、PDF 页码或 TXT 段落、字段来源、预期可见性、降级与合成必要性；升级 Ground Truth schema v2，同时保留 v1 读取兼容；`GROUND_TRUTH.md` 新增中文“来源与仿真说明”，并对模型文本做 Markdown 转义。
- 新增或更新测试：覆盖 `registry_query` 场景解析、实际故事线执行、eCAR READ 渲染、Sysmon 不冒充写入、记录级直接可见性、来源元数据 JSON/Markdown 保留、旧 v1 Ground Truth 读取和 schema v2 生成回归。
- 定向测试结果：`uv run pytest --no-cov tests/unit/test_models.py tests/unit/test_events.py tests/unit/test_ecar_spec_compliance.py tests/unit/test_ground_truth.py tests/unit/test_record_ground_truth.py tests/unit/test_storyline_registry_query.py tests/unit/test_report_compiler.py tests/unit/test_report_ingest.py tests/unit/test_spillage.py tests/integration/test_report_to_dataset_semantics.py -q`，509 个测试全部通过；另外运行一个实际生成型 spillage/schema 回归，1 个通过。
- 完整离线测试结果：本轮没有重复运行全仓 5,000 余项重型套件；已运行此工作包、报告导入、编译器、语义基线和受 schema 影响的完整定向集合。相关 Python 文件通过 Ruff 检查、格式检查和 `git diff --check`。
- 真实 MiniMax 调用次数：0。
- 来源贴合硬门槛：注册表读取已能无损物化，且不会因 Sysmon 不支持而反转为写入；来源与 AI 补全边界已能进入标准最终产物。报告命令尚未切换到新编译结果，因此尚不宣称端到端硬门槛全部达成。
- 来源贴合综合分：尚未计算，待工作包 5 将 A/B/C、编译器、场景适配和生成前后质量门串联后，再用真实 PDF/TXT 产物评估。
- 未解决问题与下一步：新编译中间表示还缺少到 `Scenario`/`source_metadata` 的适配器，旧 `ReportPipeline` 仍走单次抽取和旧转换器。工作包 5 将切换主流水线，增加生成前/后质量门与脱敏运行摘要。
- 提交：`04bdc8a feat: 支持注册表查询与来源标记`。

### 2026-08-27：工作包 5——A/B/C 端到端管线、质量门与原子发布

- 输入与运行范围：使用虚构 TXT 和本地结构化模型替身测试 A/B/C 管线；使用一个虚构进程场景实际运行原生 `GenerationEngine`，生成 Windows/eCAR、Ground Truth 和记录级标签。本轮没有读取 `1.txt`、没有调用真实 MiniMax、没有读取示例 PDF。
- 本轮假设：首轮质量要依靠固定职责、确定性编译和发布前后硬门槛，不依靠生成后再让模型临时评点或重写。
- 发现的根因：旧主管线仍使用单次 `ReportExtraction` 和旧 APT 转换器，之前已完成的 A/B/C、事实账本和编译器实际没有进入用户命令；中间编译结果还缺少到原生 `Scenario` 的适配；旧命令直接向最终目录生成，后置失败可能留下半成品或覆盖旧结果。
- 通用修改：主管线切换为 A 分块来源抽取、B 完整性建议逐条本地校验应用、C 显式缺失字段补全，每阶段最多修复一次；高置信哈希/IP 由本地合并为报告 IOC；新增中间编译结果到原生 `Scenario` 的适配器，只把已物化事件写入 storyline，将报告 IOC 和无损渲染不可行的事实保留在新的 `report_context`；进程使用稳定 `process_ref` 并保持存活；Ground Truth schema v2 保留报告 IOC 和事实落地状态。
- 质量与发布：新增生成前和生成后双质量门，按30/25/20/15/10 权重检查精确来源、行为覆盖、关系一致、IOC 保留和物理标签；硬门槛失败不发布。生成在目标同级唯一临时目录完成，通过后重命名发布；`--force` 使用先备份后替换的可恢复流程。
- 新增或更新测试：新增质量门 6 项、Scenario 适配/原生生成 3 项、管线/回滚 4 项，并更新提示词分块片段目录测试。
- 定向测试结果：`uv run pytest --no-cov tests/unit/test_report_ingest.py tests/unit/test_report_pipeline.py tests/unit/test_report_quality.py tests/unit/test_report_scenario_adapter.py -q`，59 个测试全部通过；其中原生生成引擎实跑用例同时通过端到端生成后评估和不依赖模型中间 JSON 的离线重评。
- 完整离线测试结果：受影响的场景模型、规范事件、eCAR、Ground Truth、记录级标签、报告编译/管线/质量、生成引擎、CLI、spillage 和语义集成基线共 602 个测试全部通过，耗时 3.79 秒。
- 真实 MiniMax 调用次数：0。
- 来源贴合硬门槛：虚构端到端用例的来源精确引用、补全标记、IOC 保留、行为覆盖、降级原因和记录级标签全部通过。
- 来源贴合综合分：虚构端到端正向用例的生成前/后评分均为 100.00；该分数只验证机制，不代表 APT-C-48 真实报告验收结论。
- 未解决问题与下一步：旧 `regenerate_apt_scenarios.py` 仍可被单独调用，其历史兜底要在工作包 6 收紧；工作包 7 要将三类虚构 TXT 语义基线接到完整假管线；工作包 8 才读取用户凭据并对 APT-C-48 PDF 进行三次真实 MiniMax 验收。
- 提交：`5f382a2 feat: 增加报告来源贴合质量门`。

### 2026-08-27：工作包 6——旧 APT 场景转换器危险兜底收紧

- 输入与运行范围：只修改仍可被单独调用的旧 APT JSON 转换器及其回归测试；使用测试内虚构 JSON 实际写出并用 `Scenario` 严格模型重新读取 `scenario.yaml`。没有读取 `1.txt`、没有调用 MiniMax、没有读取示例 PDF，也没有发布正式数据集。
- 本轮假设：新报告主管线已经绕开旧转换器，但旁路脚本仍可能被人工调用；因此必须让“报告没说”在旧路径中表现为显式降级，而不是看似精确的 PowerShell、HTTP URI 或进程所有权。
- 发现的根因：旧脚本把完整战役网络字典合并到每个网络步骤，导致某一行为的 HTTP 方法、URI 和服务泄漏到另一行为；缺少所有者时先沿用同一主机的上一进程，再最终虚构 PowerShell 或 Bash；只要存在注册表键就无条件追加 `registry_set`；另外还把仓库 ATT&CK v18.1 数据中仍有效的 `T1562.001`、`T1070.001` 映射成数据集中不存在的编号。
- 通用修改：战役级网络上下文只保留域名、IP 和端口，并且仅在当前步骤没有端点或端点一致时合并；HTTP 方法、URI 和服务始终停留在当前行为。删除最后进程继承和 Windows/Linux shell 最终默认值；缺失所有者时为当前步骤独立创建 `report-unspecified` 占位进程，并在描述中写入 `LEGACY_CONVERSION_DEGRADED`，明确它只是仿真脚手架。注册表操作按同一步骤的行为标签、动作字段、命令或值数据区分查询与写入，分别生成 `registry_query`、`registry_set`；语义不明确时只记录降级，不生成精确注册表事件。ATT&CK 兼容映射只保留真实退役编号映射。
- 新增或更新测试：新增 6 个针对性回归和 1 个写盘级端到端回归，覆盖跨步骤 HTTP 泄漏、不同端点禁止混合、上一进程复用、PowerShell/Bash 默认值、注册表读写区分、未知注册表语义降级，以及最终 YAML 的严格模型解析和无 PowerShell 检查。
- 定向测试结果：`uv run pytest --no-cov tests/unit/test_regenerate_apt_scenarios.py tests/integration/test_report_to_dataset_semantics.py -q`，30 个测试全部通过；旧转换器测试文件单独运行 19 个全部通过。相关文件通过 Ruff 检查、格式检查和 `git diff --check`。
- 真实 MiniMax 调用次数：0。
- 来源贴合硬门槛：旧旁路已不再跨行为继承 URI/方法、不再复用上一进程、不再把缺失主体默认为 PowerShell，也不再把注册表读取反转为写入；不可精确表达的情况在最终场景中可见地降级。
- 来源贴合综合分：本工作包不运行报告主管线，未生成新的来源贴合分；工作包 5 的虚构端到端机制分仍为 100.00，不能替代真实报告验收。
- 未解决问题与下一步：旧转换器的占位主体仍是为了满足原生事件所有权约束而存在的仿真脚手架，不应被解释为报告 IOC。工作包 7 将把简短无 IOC、纯网络行为、多任务/多文件/多 URL 三类输入全部接到完整假 MiniMax 管线和最终产物离线重评。
- 提交：`5b218f4 fix: 收紧旧 APT 场景转换器危险兜底`。

### 2026-08-28：工作包 7——报告到最终日志的离线语义回归

- 输入与运行范围：新增“简短无 IOC”“仅网络行为”“多任务/多文件/多 URL”三类脱敏 TXT 输入，以及每类对应的 A 来源抽取、B 完整性审核、C 受控补全固定响应；所有用例都从报告入口经过完整主管线并实际调用原生 `GenerationEngine`。本轮没有读取 `1.txt`、没有调用真实 MiniMax、没有读取示例 PDF，也没有发布正式数据集。
- 本轮假设：只检查中间 JSON 或场景快照不能证明最终日志贴合报告；必须从最终 `scenario.yaml`、Ground Truth 和实际日志反向检查行为、绑定关系、重复触发参数及来源披露，并验证同输入重复生成的语义稳定性。
- 发现的根因：生成前质量门把 C 阶段合法补全的进程实体误判为未知实体；下载事实虽然保留 URL，却没有把同一事实绑定的目标文件落成 `file_create`，导致最终数据丢失 URL 到文件的关系；计划任务触发周期停留在来源 attributes 中，没有进入严格场景模型和任务 XML；旧语义集成测试使用合成快照，没有穿过真实故事线、渲染器和发布质量门。
- 通用修改：质量门显式接纳带 `AI_COMPLETED` 来源的编译生成实体，同时继续拒绝未登记引用；AI 进程字段使用一致的补全来源标记。编译器新增显式 `file_create` 路由，并让下载事实把来源绑定的目标文件物化为同事实文件创建；计划任务从中英文分钟、小时和 ISO 周期中确定性解析 `interval_minutes`，严格模型限制为 1 至 525600 分钟，故事线生成相同周期的任务 XML、恶意事件属性和 Ground Truth；提示词受控行为词表同步加入 `file_create`。
- 新增或更新测试：三类正向端到端用例分别检查最终行为集合、URL/文件/任务绑定、标准产物和 100 分质量门；多任务用例进一步检查两个任务的 20/30 分钟周期和 XML；稳定性用例对同一输入独立生成两次并比较最终语义快照；篡改用例在原生生成后改写 Ground Truth IOC，确认后置质量门拒绝发布且不残留临时目录。
- 定向测试结果：报告导入、编译、质量、管线、场景适配、严格模型、事件和 Ground Truth 共 243 个单元测试全部通过，耗时 2.19 秒；提交前再次运行完整语义集成文件，16 个测试全部通过，耗时 80.70 秒。相关 Python 文件通过定向 Ruff 检查、格式检查和 `git diff --check`。
- 原生生成次数：共 6 次；其中 5 次正向/稳定性生成成功发布到测试临时目录，1 次在 Ground Truth 被测试故意篡改后按预期拒绝发布。
- 真实 MiniMax 调用次数：0；固定响应仅模拟 A/B/C 的结构化返回，不模拟模型文本推理质量。
- 来源贴合硬门槛：三类输入的精确来源、行为覆盖、关系一致、IOC 保留和物理标签均通过；无 IOC 用例不虚构 IOC，纯网络用例保持 HTTPS C2 端点，多实体用例保持每个 URL、文件和计划任务的独立绑定；同输入两次生成的最终语义一致。
- 来源贴合综合分：三类正向用例的生成前/后评分均为 100.00；该结果证明固定响应下端到端实现不再丢失已抽取语义，但不能代替真实 MiniMax 对开放输入的抽取质量验收。
- 未解决问题与下一步：工作包 8 将首次读取用户提供的凭据，对 APT-C-48 PDF 在三个全新目录中独立运行，逐次记录 MiniMax 调用摘要、硬门槛、综合分和最终产物语义矩阵；若发现通用根因，则先补离线回归再修复并单独提交。
- 提交：本次使用 `test: 增加报告到日志语义回归测试`。

### 2026-08-28：工作包 8 第 1 轮——跨页实体引用确定性重定位

- 真实输入：首次读取用户提供的 `1.txt`，凭据只交给 `SecretStr`，终端、日志、仓库和模型调用摘要均未打印密钥；示例 PDF 本地提取为 8 页、3,581 个规范化字符和 10 个来源片段，确认存在可复制文字层。
- 运行结果：`apt-c48-run-01` 在 MiniMax A 来源抽取阶段被本地质量校验拒绝，未创建或发布输出目录。首次结构化返回与一次定向修复共发生 2 次真实模型调用；两次都没有越过来源硬门槛。
- 发现的根因：PDF 中一个计划任务表格跨页，任务名和二进制路径位于不同来源片段。模型把它们正确组织为同一实体，却只引用了部分片段；原校验器要求实体 value 和所有编译器会使用的 attributes 都必须在同一组引用中逐字出现，因此合法的跨页实体在一次修复后仍被拒绝。
- 通用修改：A 阶段解析后由本地代码逐项重定位实体 value、编译器使用的技术 attributes 和报告 IOC；仅当精确规范化值确实存在于某个本地来源片段时才补入该片段 ID，报告不存在的值保持原样并继续失败。中文提示词同步要求跨页/跨段实体列出覆盖全部精确值的来源片段。
- 回归证明：新增“任务名与二进制跨片段”正向用例和“虚构二进制仍拒绝”反向用例；报告导入与管线测试 52 个全部通过，完整报告到原生日志语义集成测试 16 个全部通过，耗时 79.93 秒；相关 Ruff、格式和差异检查通过。
- 下一步：提交该通用根因修复后，在仍为空且从未发布的 `apt-c48-run-01` 重新执行完整命令；它不计入连续三次成功验收。
- 提交：本次使用 `fix: 修复跨页报告实体来源引用`。

### 2026-08-28：工作包 8 第 2 轮——实体规范值收回本地计算

- 运行结果：修复跨页引用后重新运行 `apt-c48-run-01`，MiniMax A 一次通过；MiniMax B 的首次审核和一次定向修复都因新增实体携带错误 `canonical_value` 被严格模型拒绝，流水线再次未创建或发布输出目录。本轮发生 3 次真实模型调用（A 1 次、B 2 次）。
- 发现的根因：`canonical_value` 是实体去重和比较使用的本地派生键，仓库已经有按 `entity_type + value` 计算的唯一算法，但该字段仍暴露在 MiniMax JSON Schema 中；模型会复制、改写或自行大小写规范化，导致本地算法与模型结果冲突。继续要求模型修复一个它不应负责的字段没有意义。
- 通用修改：从模型可见的 `ReportEntity` JSON Schema 中移除 `canonical_value`，模型提示词明确禁止输出；Pydantic 实体仍保留该字段用于内部序列化和历史产物读取，但无论外部是否传值，都由本地规范化算法覆盖。实体原始 value、类型和来源引用仍照常严格校验，因此没有放宽来源边界。
- 回归证明：新增“错误外部规范值被本地覆盖且 Schema 不暴露字段”的测试；报告导入、管线和严格场景模型共 147 个测试全部通过，完整报告到日志语义集成测试 16 个全部通过，耗时 79.55 秒；相关 Ruff、格式和差异检查通过。
- 下一步：提交后第三次从仍为空的 `apt-c48-run-01` 执行；前两次失败均不计入连续三次成功验收。
- 提交：本次使用 `fix: 将实体规范值改为本地派生`。

### 2026-08-28：工作包 8 第 3 轮——来源精确字段本地清理与周期原句恢复

- 运行结果：收回实体规范值后再次运行 `apt-c48-run-01`，MiniMax A 首次返回和一次定向修复都把报告语义概括写进了必须逐字取自原文的实体或技术属性，例如概括性的样本名称和改写后的任务触发描述；本地来源门拒绝，未创建或发布输出目录。本轮发生 2 次真实模型调用。
- 发现的根因：事实 description 允许忠实概括，但实体 value、aliases、任务二进制/触发器、注册表值等会进入最终场景和日志，必须是精确来源主张。提示词虽已要求原样摘录，开放输入下模型仍会把两类字段混用；仅依赖一次模型自修复无法稳定解决。
- 通用修改：A 返回后新增确定性来源清理。实体 value 在所有合法重定位后仍无精确命中时，删除该实体及引用它的关系，但保留来源事实供 Ground Truth/降级判断；无精确命中的别名和技术属性逐项删除；报告 IOC 无精确命中时删除。计划任务 trigger 是唯一允许的受控恢复：只有模型概括和引用片段中的唯一原句解析出相同明确周期时，才复制原句，绝不使用模糊相似度或模型改写。
- 提示词修改：进一步区分“描述可概括”和“精确字段必须逐字摘录”，找不到逐字值时要求省略实体、别名或属性。
- 回归证明：新增概括实体及关系被安全移除、同周期触发器恢复原文两项回归；报告导入与管线测试 55 个全部通过，完整报告到日志语义集成测试 16 个全部通过，耗时 80.40 秒；相关 Ruff、格式和差异检查通过。
- 下一步：提交后第四次从仍为空的 `apt-c48-run-01` 执行；前三次失败均未发布，仍不计入连续三次成功验收。
- 提交：本次使用 `fix: 清理无原文支撑的报告精确字段`。

### 2026-08-28：工作包 8 第 4 轮——机械式进程补全改由本地引擎生成

- 正常运行结果：精确字段清理后，`apt-c48-run-01` 的 A/B/C 均一次通过并到达生成前质量门，但 15 个来源事实中仅 1 个被当前评分视为已物化，覆盖率 6.7%、总分 76.67，因此正确拒绝发布。本轮发生 3 次真实模型调用。
- 脱敏诊断：为区分关系/编译/评分问题，额外执行两次只输出行为类型、状态和降级原因的诊断，不保存模型响应、实体值、IOC 或报告原文，也不进入生成器。第一次在 B 修复后仍失败（A 1 次、B 2 次）；第二次 B 修复后通过，但 C 首次及修复均把 `process.image` 选成报告未披露的命令解释器，被编译器拒绝（A 1 次、B 2 次、C 2 次）。所有诊断目录均未发布。
- 发现的根因：当前编译器产生的补全请求全部是 `process.image`，其作用仅为给已知来源行为建立 EvidenceForge 内部事件所有权；该路径无需重新理解报告。把它交给 MiniMax 会随机生成 CMD/PowerShell 等解释器，既不稳定又违反来源边界。
- 通用修改：新增本地机械补全分流。`process.image` 使用事实 ID 和受控 behavior 生成稳定的 `C:\\ProgramData\\EvidenceForge\\ReportSim\\...exe`，明确标记为 `engine_generated`，不含命令解释器、外部 IOC 或新增行为；只有未来出现无法由本地规则解决的白名单请求才调用 MiniMax C。新增 `partially_engine_generated` 来源类别，并贯通严格 Scenario、编译事件和 Ground Truth 中文披露，避免把本地值冒充报告或 AI 输出。
- 调用行为：对于当前报告和三类离线 TXT，主管线只需要 MiniMax A/B 两个语义阶段；C 契约、提示词和校验仍保留，但补全请求为空时不再产生无意义外部调用。
- 回归证明：新增稳定引擎占位、禁止解释器、引擎来源类别和 C 按需跳过断言；编译、管线、严格模型、Ground Truth 与质量门共 177 个测试全部通过，完整报告到日志语义集成测试 16 个全部通过，耗时 79.48 秒；相关 Ruff、格式和差异检查通过。
- 下一步：先提交该独立根因修复，再运行脱敏编译矩阵。若覆盖率低来自 Ground Truth-only 事实被错误计入“可物化”分母，修复评分定义；若来自实体关系丢失，则继续修复 A/B 合并或编译路由。
- 提交：本次使用 `fix: 使用本地安全进程完成机械补全`。

### 2026-08-28：工作包 8 第 5 轮——事实分块与唯一可证关系补齐

- 脱敏矩阵结果：本地机械补全提交后再次诊断，A 和 B 各使用一次有界修复，共 4 次真实调用；得到 13 个受控/非受控事实、46 个实体但只有 6 条关系。3 个进程事实可物化，其余网络下载/C2、计划任务、注册表和删除事实主要因实体未绑定而 Ground Truth-only；一个 `campaign_attribution` 还被模型错误当作攻击行为。覆盖率 23.1%、总分 80.77，未发布。
- 发现的根因：A 单次处理整份报告时输出了大量反分析目标进程，容易遗漏 URL/任务/注册表到事实的关系；B 虽能新增实体，但真实运行中经常没有把孤立技术实体补成关系。另一方面，本地已经验证并保留的报告 IOC 只停留在 `report_iocs`，没有提升为编译器可消费的实体。关系缺失不能通过降低质量门解决。
- 通用修改：A 阶段改为约 2,000 字符、零重叠的段落边界分块，避免一个结构化输出承载整份长报告；提示词明确“IOC/实体必须同时绑定 fact”，B 明确核对孤立技术实体且不得把反分析目标进程当行为主体。本地把已通过来源验证的报告 IOC 确定性提升为实体；当一个实体在同一来源片段中只有一个兼容事实时，按类型建立下载、网络、任务、文件或注册表关系。若同一片段存在两个兼容网络事实则保持未绑定，禁止猜测和 URI 串绑。非受控的归因/背景标签不再作为攻击行为进入评分分母。
- 回归证明：新增报告 IOC 到唯一下载事实、歧义网络 IOC 不绑定、两个任务同时绑定、实体间 `resolves_to` 保留及非行为事实移除测试；报告导入、管线和编译器 71 个测试全部通过，提交前再次运行完整报告到日志语义集成测试，16 个全部通过，耗时 78.87 秒；相关 Ruff、格式和差异检查通过。
- 下一步：提交后使用真实 PDF 重新运行脱敏矩阵，验证两分块抽取是否恢复 URL、计划任务和 BIOS 查询。仍缺失的行为按提示词、确定性关系或编译事件能力继续归因，不降低硬门槛。
- 提交：本次使用 `fix: 补齐唯一可证的报告实体关系`。

### 2026-08-28：工作包 8 第 6 轮——MiniMax 限流有界退避

- 运行结果：实体关系修复提交后启动两分块真实诊断，第一块尚未完成便收到 MiniMax 429；客户端内部旧重试耗尽，完成模型调用记录为 0，没有进入 B、编译或发布阶段。
- 发现的根因：旧客户端虽把 429 识别为可重试，却只等待 1 秒和 2 秒，并忽略 HTTP `Retry-After`，无法覆盖真实账户/服务限流窗口。立即由外层重跑还会进一步放大限流。
- 通用修改：安全 HTTP 异常新增不含响应正文的 `retry_after_seconds`；标准库传输只解析数值型 `Retry-After` 并上限截为 60 秒。无提示时采用 5/10/20 秒、最多 3 次重试的有界指数退避；鉴权和其他不可重试错误仍立即失败，密钥、请求和响应内容仍不进入日志。
- 回归证明：报告导入/MiniMax 客户端 54 个测试全部通过；新增 429 按 7 秒服务端提示等待并成功重试的断言，相关 Ruff、格式和差异检查通过。
- 下一步：提交后再执行真实脱敏矩阵。若服务持续 429，则保留当前干净状态并等待外部限流窗口，不通过并发或无限重试绕过。
- 提交：本次使用 `fix: 完善 MiniMax 限流退避`。

### 2026-08-28：工作包 8 第 7 轮——从抽取结果到可执行事件的安全补全

- 凭据切换：用户判断 `1.txt` 可能到达 5 小时限额，后续真实调用改为读取项目外的 `2.txt`。只验证凭据解析成功，密钥、基地址和模型原始响应均未打印、未写入仓库。
- 真实脱敏诊断：新凭据完整跑通 2 个 A 分块和 1 个 B 审核；A 第一块和 B 各使用一次定向修复，共 5 次真实调用。结果保留 14 个受控事实、8 个报告 IOC（3 个哈希、1 个 IP、4 个 URL）、反分析目标进程、注册表、计划任务和下载实体；但仅 5/14 个事实至少生成一个可执行事件，覆盖率 35.7%、生成前得分 83.93，正确拒绝发布。
- 端到端根因：这次抽取已基本保住报告中的主要技术事实，继续叠加 MiniMax 调用不会让既有编译器自动获得缺失的事件能力。九个降级事实分为：网络事实缺唯一 IOC 绑定 3 个、反调试/反虚拟机缺事件模板 2 个、自删除缺显式文件关系 2 个、鱼叉邮件缺收件地址 2 个。
- 通用修改：未能唯一绑定报告 IOC 的网络事实使用由事实 ID 稳定派生的 RFC 5737 保留地址，每个端点、端口、服务和 HTTP 方法均标记 `engine_generated`，不进入报告 IOC。反分析事实只把同一来源片段内唯一可证的进程和注册表键绑为检查目标，分别物化为最小查询权限的 `process_access` 和 `registry_query`；没有可证目标时仍降级。自删除在没有源文文件路径时删除受控生成的自身进程镜像，避免误删来源命令解释器。邮件缺收件人时使用现有隔离实验环境内部邮箱，不猜测外部地址。
- 来源披露：所有安全补全字段均进入 `partially_engine_generated` 来源类别和字段级原因；报告精确 IOC、进程名和注册表键继续保持 `source`，没有通过放宽来源校验或降低 90% 覆盖率门限过关。
- 回归证明：新增反分析目标、无绑定网络端点、无文件自删除、无收件人鱼叉邮件和反分析自动关系 5 组回归；编译与报告导入测试 72 个全部通过，管线、质量门和完整原生生成语义集成测试 27 个全部通过，集成耗时 78.26 秒；相关 Ruff、格式和差异检查通过。
- 下一步：提交该通用修复后，使用 `2.txt` 在新目录中重新跑真实脱敏矩阵；确认生成前覆盖率通过后，再运行原生生成器和生成后质量门。
- 提交：本次使用 `fix: 补齐报告行为的安全仿真模板`。

### 2026-08-28：工作包 8 第 8 轮——审核关系使用可验证派生来源

- 运行结果：安全仿真模板提交后，使用 `2.txt` 重跑两分块脱敏诊断。A 第二块使用一次定向修复；B 首次与修复响应均未通过，共 5 次真实调用，没有进入编译和发布。
- 发现的根因：B 识别到了孤立技术实体并建议补齐 11 条关系，但把语义绑定诚实地标为 `derived`；旧本地校验却对 fact、entity 和 relation 使用同一条“只允许 source”规则。关系是对两个来源项之间语义的判断，强制写成逐字来源反而会模糊来源边界。
- 通用修改：B 新增事实和实体继续必须为 `source`；新增/替换关系可为 `source` 或 `derived`。`derived` 关系必须使用受控谓词，引用已存在的两端，两端必须共享建议所引来源片段；若提供 `source_ids`，必须精确等于两端 ID。B 提示词升级为 `report-audit-v3`，明确事实/实体与语义关系的来源区别。
- 安全边界：该修改不允许 B 生成 `ai_completed`/`engine_generated` 的事实、实体或关系，也不允许非受控谓词、未知端点或不共享来源片段的错绑。
- 回归证明：更新“新增实体后再添加关系”顺序用例，并新增错误端点 `source_ids` 拒绝用例；报告导入与管线 60 个测试全部通过，完整原生语义集成测试 16 个全部通过，耗时 80.66 秒；相关 Ruff、格式和差异检查通过。
- 下一步：提交后使用 `2.txt` 再跑真实脱敏矩阵，确认 B 在一次有限修复内能通过严格绑定校验，然后检查编译覆盖率。
- 提交：本次使用 `fix: 允许审核添加可验证派生关系`。

### 2026-08-28：工作包 8 第 9 轮——可物化行为分母与计划任务安全补全

- 脱敏矩阵结果：审核关系规则修复后，使用 `2.txt` 再跑真实 PDF。两个 A 分块均一次通过，B 使用一次定向修复，共 4 次真实调用；得到 26 个来源事实、9 个报告 IOC（1 域名、3 哈希、1 IP、4 URL）和 54 条关系。安全模板使 18 个事实物化，生成前得分 92.31，但旧分母仍按全部 26 个事实计算，覆盖率 69.2%，正确未发布。
- 发现的根因：5 个事实的 behavior 是模型明确标注的 `other`，表示无法进入受控事件模板的上下文；它们应留在 Ground Truth，但不应被称为“可物化行为”的分母。其余降级中，2 个反分析事实没有任何可证目标，继续降级是正确的；1 个事实明确说存在计划任务，但精确任务名没有唯一绑定，属于可安全补全的实验字段。
- 质量定义修正：生成前 `materializable_behavior_coverage` 只将受控行为纳入分母；`other` 事实仍保留在账本、Scenario `report_context` 和 Ground Truth，仍须通过精确来源与降级原因检查。若报告只有 `other`、不含任何受控行为，覆盖率按 0% 并硬失败，不能利用空分母过关。90% 覆盖率门限未降低。
- 计划任务补全：对 behavior 已明确是 `persistence_scheduled_task`、但无唯一任务实体的事实，根据 fact ID 生成稳定的 `EvidenceForge_ReportTask_<digest>` 隔离实验任务名，以 `engine_generated` 进入字段来源，不冒充报告任务名。
- 关系谓词归一：真实运行出现 4 条模型同义词 `connected_to`，本地确定性归一为编译器受控谓词 `connects_to`，关系来源改为引用两端的 `derived`，不把模型词形当成报告原文。
- 回归证明：新增缺名计划任务、关系谓词归一、`other` 保留但不进分母、全 `other` 硬失败回归；编译、导入、质量与管线 87 个测试全部通过，完整原生语义集成测试 16 个全部通过，耗时 77.29 秒；相关 Ruff、格式和差异检查通过。
- 预期验证：按本轮脱敏矩阵，修正后受控行为分母为 21，计划任务补全后至少 19 个可物化，覆盖率 90.5%；实际结果仍必须由下一次独立真实运行证明。
- 下一步：提交后直接在全新 `apt-c48-run-01` 目录执行正式管线，运行原生生成器、生成后质量门、标准 Scenario 校验和离线来源重评。
- 提交：本次使用 `fix: 区分可物化行为与报告上下文`。

### 2026-08-28：工作包 8 第 10 轮——审核关系建议引用本地重定位

- 正式运行结果：首次启动 `apt-c48-run-01` 正式管线时，两个 A 分块完成，B 首次未通过后进入一次定向修复；修复结果的第 9 条关系建议仍遗漏建议外层 `source_span_ids`，因此在 B 阶段被拒绝。没有进入编译/生成，没有创建或发布 `apt-c48-run-01`。
- 发现的根因：关系建议同时含有“建议外层引用”和“关系自身来源”；模型可正确给出两端、谓词和 `derived source_ids`，却在多条批量建议中偶发遗漏外层片段列表。让模型再重写整批建议会引入新波动，而该引用可由已验证的两端机械恢复。
- 通用修改：只对 `ADD_RELATION`/`REPLACE_RELATION` 放宽 Pydantic 的“必须当场含建议片段”结构限制；进入语义校验前，按建议顺序索引已有或前序新增的事实/实体端点。仅当关系两端共享至少一个已验证来源片段时，把共享片段填回建议外层；不修改关系两端、谓词或 provenance。
- 安全边界：`ADD_FACT`、`ADD_ENTITY` 和 `SPLIT_FACT` 仍必须由模型显式提供真实片段，本地不会自动补。关系两端没有共享片段时仍硬失败，因此不会用该机械修复掩盖跨段错绑。
- 回归证明：新增“共享片段可重定位”正向用例和“无共享片段仍拒绝”反向用例；报告导入与管线 63 个测试全部通过，完整原生语义集成测试 16 个全部通过，耗时 80.73 秒；相关 Ruff、格式和差异检查通过。
- 下一步：提交后仍使用不存在的 `apt-c48-run-01` 目录重启正式管线，不使用 `--force`。
- 提交：本次使用 `fix: 重定位审核关系的缺失引用`。

### 2026-08-28：工作包 8 第 11 轮——B 审核恢复逐条顾问模式

- 正式运行结果：关系建议重定位提交后再次启动 `apt-c48-run-01`。A 完成两个分块，B 首次未通过后执行一次整批定向修复；修复响应包含多条两端无共享片段的 `derived` 关系，以及非受控谓词 `communicates_with` 和 `identified_by`，因此整批被拒绝。没有创建或发布正式目录。
- 发现的根因：数据契约将 B 定义为“不改写账本的审核建议”，本地代码也按建议顺序逐条校验；但旧管线把“任意一条建议无效”升级成“整个 B 响应无效”，并要求模型重写全部建议。开放输入下，整批重写会把已正确建议替换成新错误，不符合“本地最终决定”的设计。
- 通用修改：`apply_fact_audit_suggestions` 新增显式 `strict` 模式。严格模式保留原行为，供独立审计、测试和问题诊断；主管线使用顾问模式，Pydantic 结构契约通过后不再因语义建议错误重写整批，而是按当前账本逐条校验：合法建议立即应用，无效建议直接忽略，后续建议继续基于已接受结果校验。
- 安全边界：顾问模式只改变“单条拒绝是否拖垮整批”，没有改变单条建议的任何来源、精确值、端点、谓词或共享片段规则。非受控谓词和错绑仍不会进入账本；最终账本仍必须通过编译器与生成前/后硬质量门。
- 调用收益：B 的语义建议错误不再触发一次价格高、波动大的整批修复调用；只有工具 JSON/Pydantic 结构本身不合法时才使用一次有限修复。
- 回归证明：新增同批建议“合法 `performed_by` 应用、非受控 `communicates_with` 丢弃”用例，并确认严格模式仍报错；导入、管线与质量 72 个测试全部通过，完整原生语义集成测试 16 个全部通过，耗时 78.81 秒；相关 Ruff、格式和差异检查通过。
- 下一步：提交后重启 `apt-c48-run-01`。此后 B 只贡献通过本地硬规则的建议，正式生成是否发布只由最终账本和生成前/后质量门决定。
- 提交：本次使用 `fix: 按建议逐条应用审核结果`。

### 2026-08-28：工作包 8 第 12 轮——B 审核截断不阻断已验证 A 账本

- 运行结果：逐条顾问模式提交后，第三次启动 `apt-c48-run-01`。A 第一分块使用一次结构修复，B 也因工具结构本身不合法使用一次修复；最终账本进入编译与生成前门，得分 94.12，但受控行为覆盖率 76.5%，正确未发布。
- 诊断结果：为定位未物化行为启动一次脱敏矩阵。两个 A 分块都一次完成；B 输出达到 12,000 `max_tokens` 上限，服务明确标记截断，因此客户端没有返回或解析半截 JSON，诊断在 B 终止。
- 发现的根因：B 是可选完整性顾问，A 的两个来源分块已分别通过精确值、片段引用、实体和 IOC 校验。旧客户端把截断与鉴权、网络、限流等所有错误合并为通用 `MiniMaxError`，主管线无法对“不完整顾问输出”做安全降级。
- 通用修改：新增 `MiniMaxOutputTruncatedError`，Anthropic `stop_reason=max_tokens` 和 OpenAI `finish_reason=length` 统一映射到该类型，不返回被截断内容。A 或 C 遇到该错误仍硬失败；只有 B 遇到时，明确丢弃整个不完整审核，不应用任何半截建议，继续使用已验证 A 账本和本地确定性丰富。
- 调用计数：`ReportPipeline` 将每次逻辑 `complete` 尝试置于 `finally` 计数，因此服务已完成但输出被截断的 B 也会进入脱敏调用数，不会伪装成“未调用”。
- 安全边界：只精确捕获专用截断异常；401/403、429、网络、响应 JSON 错误和 A 阶段截断均不会被吞掉。B 跳过不能直接发布，A 账本仍必须通过生成前来源、行为覆盖、关系、补全标记和生成后物理标签硬门。
- 回归证明：新增 Anthropic/OpenAI 截断分类及 B 截断后使用 A 账本完整发布的管线用例；导入、管线与质量 74 个测试全部通过，完整原生语义集成测试 16 个全部通过，耗时 79.51 秒；相关 Ruff、格式和差异检查通过。
- 下一步：提交后重新运行脱敏矩阵。若 B 再被截断，矩阵仍能输出 A + 本地编译的具体未物化行为；完成通用模板修复后再重启正式 `run-01`。
- 提交：本次使用 `fix: 审核输出截断时保留已验证账本`。

### 2026-08-28：工作包 8 第 13 轮——受控进程行为统一补全执行主体

- 脱敏矩阵结果：B 截断降级提交后，使用 `2.txt` 再次运行矩阵。两个 A 分块和 B 均一次完成，无修复调用；获得 18 个来源事实、9 个报告 IOC（1 域名、3 哈希、1 IP、4 URL）和 55 条关系。一个 `other` 事实只保留在 Ground Truth；17 个受控行为中 15 个物化，覆盖率 88.2%、得分 97.06，正确未发布。
- 未物化事实：第一个是 `anti_debug_anti_vm`，没有关联任何可证目标进程或注册表键，继续 Ground Truth-only 是正确的来源约束。第二个是 `host_recon`，报告明确描述主机侦察行为，但没有披露执行进程或精确命令；旧 `_PROCESS_COMPLETION_BEHAVIORS` 只允许诱饵打开、恶意软件执行和下载后执行生成内部进程，与其他同样映射为 `PROCESS` 的受控行为不一致。
- 通用修改：所有由 `_PROCESS_BEHAVIORS` 路由的受控行为，在行为本身有来源、但执行进程和命令都缺失时，统一由本地引擎生成稳定的隔离实验进程。进程仍使用既有 `C:\ProgramData\EvidenceForge\ReportSim\...exe` 算法，标记 `engine_generated`，不含命令行，不选择 CMD/PowerShell/WScript/CScript，不增加外部 IOC 或新行为。
- 反分析例外：`anti_debug_anti_vm` 继续由专用编译器处理，必须有来源目标进程或注册表键才物化；不使用通用进程主体补全来伪造反分析目标。本轮矩阵中另一个 BIOS 注册表反分析和一个双进程目标反分析已分别物化为 `registry_query` 和 `process_access`。
- 回归证明：新增无主体 `host_recon` 使用引擎进程物化用例，明确无 `command_line`、无命令解释器且来源为 `partially_engine_generated`；编译、质量和管线 32 个测试全部通过，完整原生语义集成测试 16 个全部通过，耗时 81.26 秒；相关 Ruff、格式和差异检查通过。
- 预期验证：按本轮矩阵，修正后 17 个受控行为中将有 16 个物化，预期覆盖率 94.1%；仍由下一次独立正式运行验证，不复用当前模型输出。
- 下一步：提交后重启正式 `apt-c48-run-01`，完成原生生成、生成后硬门、标准校验和独立离线重评。
- 提交：本次使用 `fix: 为受控进程行为补全内部主体`。

### 2026-08-28：工作包 8 第 14 轮——B 两次结构失败后安全跳过

- 正式运行结果：受控进程行为补全提交后重启 `apt-c48-run-01`。A 完成两个分块；B 首次输出的 `suggestions` 不是 JSON 列表，唯一修复响应仍不是列表，因此 `_call_structured` 按设计抛出 `ReportModelOutputError`。旧 B 调用边界只对服务截断做安全跳过，结构修复耗尽后仍终止主链；没有创建或发布正式目录。
- 发现的根因：“被 max_tokens 截断”和“首次+唯一修复都无法形成有效 B 结构”的安全含义相同：都不存在可逐条审核的完整建议集，必须丢弃整份 B，但不应废弃已经通过精确来源校验的 A 账本。
- 通用修改：B 专用调用边界精确捕获 `MiniMaxOutputTruncatedError` 或该 B 的 `ReportModelOutputError`，记录“未形成完整有效审核”，构造一个不应用任何建议的本地空审核结果，继续 A 账本。A/C 的 `ReportModelOutputError` 仍上抛，B 的鉴权、限流、网络或其他 `MiniMaxError` 仍上抛。
- 安全边界：首次和修复响应均不会被部分解析、宽松转换或拼接；它们的所有建议均为零应用。跳过 B 后仍必须通过本地 IOC 合并、唯一关系丰富、编译、受控补全和生成前/后硬门。
- 回归证明：新增 B 首次 `suggestions={}`、修复 `suggestions="invalid"` 的双结构失败管线用例，确认最终使用 A 账本发布、调用数为 A+B+B修复共 3 次且修复标记存在；导入、管线与质量 75 个测试全部通过，完整原生语义集成测试 16 个全部通过，耗时 80.92 秒；相关 Ruff、格式和差异检查通过。
- 下一步：提交后重启正式 `apt-c48-run-01`。B 的三种结果——有效建议、有效结构内的无效建议、无完整有效结构——均已有确定性本地处理。
- 提交：本次使用 `fix: 审核结构失败时继续已验证主链`。

### 2026-08-28：工作包 8 第 15 轮——反分析目标进程的真实记录物化

- 正式运行结果：B 结构失败跳过提交后，`apt-c48-run-01` 首次完整走过 A/B、生成前质量门、标准 Scenario 验证和原生生成器。Scenario 含 69 个故事线事件、15 个人员与 2 个系统；原生生成器产生约 5 MB `RECORD_GROUND_TRUTH.jsonl` 及完整日志。生成后总分 94.64，但 `fact-0005` 的 20 个 `process_access` 事件没有进入恶意记录标签，因此原子发布门正确删除临时目录，正式目录仍未发布。
- 发现的端到端根因：编译器已生成进程访问意图，但只在仿真状态中启动了检查者进程；报告中被检查的目标进程只有名称。原生故事线引擎又把非系统进程名固定回退为 PID 636，而不查找故事线已动态创建的实际 PID；状态管理器因此拒绝生成 Sysmon Event 10。这不是评分门误判，而是实际物理记录缺失。
- 通用修改：编译器先为每个来源目标进程生成带 `fully_synthetic` 和必要性原因的启动桥接，再启动检查者，从而同时保证目标 PID 存活且“最后进程”仍是正确访问主体。状态管理器新增按完整镜像路径/可执行文件名查找同主机运行 PID；故事线对已编种系统进程保持旧语义，对非系统目标优先使用动态 PID，最后才使用旧回退。`create_remote_thread` 也共享这一修复。
- 来源边界：目标进程名仍是报告 `source`；“在仿真中启动它”只是 EvidenceForge 产生真实访问记录的内部桥接，明确标为完全合成，不声称原报告观测到目标进程启动。访问权限仍是标记原因的最小查询权限。
- 回归证明：新增原生生成器回归，明确要求反分析 `process_access` 同时出现在 Ground Truth 和恶意记录标签；编译、状态、既有 PID 范围与真实生成共 113 个测试全部通过，完整报告到日志语义集成测试 16 个全部通过，耗时 77.65 秒；相关 Ruff 通过。
- 下一步：提交后使用仍不存在的 `apt-c48-run-01` 从头运行正式管线，验证反分析目标的每条物理记录通过生成后硬门。
- 提交：本次使用 `fix: 为反分析访问建立真实目标进程`。

### 2026-08-28：工作包 8 第 16 轮——去武器化 IOC 的展示值与执行值分离

- 正式运行结果：目标进程修复提交后再次运行 `apt-c48-run-01`。A 两个分块、B、确定性编译和生成前质量门已完成；严格 Scenario 校验在 `connection.hostname=panbaiclu[.]com` 时拒绝。原生生成器未启动，正式目录未创建或发布。
- 发现的根因：报告会用 `[.]`、`(.)`、`{.}` 和 `hxxp/hxxps` 展示 IOC。账本的 `value` 正确保留这一原文形式，`canonical_value` 也已在本地正确还原；但域名和 IP 编译分支仍把展示值直接写入原生 Scenario，混淆了“引用报告”与“产生网络记录”两种用途。
- 通用修改：新增网络执行值适配层。域名/IP 没有去武器化或大小写差异时继续使用 `source`；有差异时使用已由本地严格算法得到的 `canonical_value`，字段标为 `derived`、引用原实体 ID，并记录“只为可执行网络值恢复”原因。该逻辑统一用于 `connection.hostname`、`connection.dst_ip`、DNS `query` 和 `answer`。
- 保留语义：报告账本、`report_context`、报告 IOC 和 Ground Truth 仍保留原文 `panbaiclu[.]com`、`158.255.215[.]248` 等展示值；只有原生日志的执行字段使用 `panbaiclu.com`、`158.255.215.248`。这一还原不依赖 MiniMax 自由改写，也不允许补全打码或缺失 IOC。
- 回归证明：新增同时覆盖去武器化域名/IP 的连接与 DNS 编译测试，断言原实体不变、执行值合法且派生链完整；编译、报告导入与原生适配 85 个测试全部通过，完整报告到日志语义集成测试 16 个全部通过，耗时 80.23 秒；相关 Ruff 通过。
- 下一步：提交后再次从不存在的 `apt-c48-run-01` 完整重跑，不复用本轮 MiniMax 结果。
- 提交：本次使用 `fix: 分离去武器化 IOC 的展示与执行`。

### 2026-08-28：工作包 8 第 17 轮——反分析检查目标的确定性恢复与条件去重

- 正式运行结果：去武器化 IOC 修复提交后，再次独立运行 `apt-c48-run-01`。B 使用一次定向结构修复；最终生成前总分 97.37，但受控行为仅 17/19 物化，覆盖率 89.5% 低于 90% 硬门，正确拒绝且未发布正式目录。
- 独立复现：另运行一次不保存模型响应、不输出报告原文/IOC/实体值的脱敏编译矩阵。该轮发生 4 次逻辑模型调用，得到 20 个事实（其中 1 个 `other`），再次稳定复现 17/19、89.5%。唯二未物化项均是 `anti_debug_anti_vm`，且都缺少可证目标；其余邮件、执行、下载、主机侦察、C2、任务和自删除均已物化。
- 原报告根因：第 4 页同一来源片段明确写了“遍历进程列表比对进程名”，列出多个 `.exe` 目标，并另有 `SystemBiosVersion` 注册表查询。MiniMax 有时拆成多个反分析事实，却漏建表格中的进程实体；第 5 页“如果处于调试/虚拟机则自毁”又会被重复抽成一个没有新检查动作的反分析事实。这是表格技术值漏抽与条件后果过度拆分的组合问题。
- 提示词修正：A 升级为 `report-facts-v3`，明确 `anti_debug_anti_vm` 必须是报告实际执行的检查动作且绑定精确目标；条件句只抽其明确后果。B 升级为 `report-audit-v4`，专门检查无目标反分析项和同一检查的重复拆分。
- 本地确定性修正：对确实含反分析事实的来源片段，用受限 `.exe` 词法提取表格中的精确进程原值，以 `source` 片段建立进程实体；在同片段多个反分析概括中，按描述中的“进程/遍历/调试器”与“注册表/BIOS”分别选择唯一承载事实，并排除已是 `performed_by` 主体的进程。没有新检查目标、但同片段已有有目标检查，或与条件自删除共享片段的项，不删除，而是降为 `other` 上下文并写入不确定性原因。
- 安全边界：本地只提取来源片段中逐字存在的 `.exe`，不生成进程名，不把行为主体当检查目标；降为上下文的项仍保留在账本、Scenario `report_context` 和 Ground Truth，不用伪造进程或注册表来凑覆盖率。
- 回归证明：新增“漏抽两个进程目标 + 多个反分析事实 + 条件自毁”组合回归，断言进程目标恢复、进程/注册表事实分开绑定、条件项保留为 `other`；导入、编译、管线和原生适配 92 个测试全部通过，完整报告到日志语义集成测试 16 个全部通过，耗时 78.42 秒；相关 Ruff 通过。
- 下一步：提交后再次从不存在的 `apt-c48-run-01` 完整运行，检查新提示词和本地恢复是否在一轮独立生成中同时达到高覆盖与高物理标签率。
- 提交：本次使用 `fix: 恢复反分析目标并去除条件重复`。

### 2026-08-28：工作包 8 第 18 轮——用来源语义基线修复内部满分盲区

- 已发布诊断样本：第 17 轮提交后，`apt-c48-run-01` 首次完整发布。原生管线得到 13 个事实、97 个事件、100% 受控行为覆盖率和 100 分，5 次模型调用；标准 Scenario 校验、生成后物理标签门和离线内部重评均通过。
- 独立来源复核结论：该样本不能计入最终三次成功结果。对照原报告的独立语义基线发现四个实质问题：最终任务事件缺失任务二进制，且只保留一个任务名；诱饵文档 `Architecture.pdf` 的 URL 被错误绑定为 C2；报告发布平台域名被误收为攻击 IOC；报告明确描述的用户名、进程列表和模块列表外传未形成独立事实。说明“内部满分”只能证明产物忠于模型形成的账本，不能证明账本完整忠于源报告。
- 根因定位：原报告的计划任务表跨页，任务行、触发周期和 COM 创建方式分散在相邻文本块；模型关系又会把同段全部网络值宽泛绑定到 C2。旧确定性层只做值校验和局部唯一关系补充，没有处理跨页表格结构、文档 URL 的角色冲突、发布来源域名的上下文过滤，也没有恢复同一片段中明确的“收集对象 + 发送动作”。
- 端到端修改：在已验证来源片段上增加四类确定性结构恢复。第一，围绕计划任务事实读取有限相邻片段，恢复精确任务名、任务二进制、触发原文和创建方式；第二，文档后缀 URL 从 C2/外传关系解绑，并绑定唯一诱饵下载事实；第三，只有被技术事实引用、作为技术 URL 主机、处于明确 IOC 上下文或使用去武器化写法的域名/URL 才保留为报告 IOC；第四，同片段同时明确列出主机清单项和发送动作时，恢复 `c2_system_info_exfil` 来源事实。
- 原生产物贯通：计划任务 `creation_method` 新增受控值 `com/schtasks/unspecified`，由来源原文确定性归一后进入 Scenario、故事线恶意事件和 Ground Truth；中文“每隔 N 分钟/小时”可确定性换算为 `interval_minutes`。这些字段会实际影响并出现在最终项目输出，不是只存在于中间 JSON 或评分报告。
- 安全边界：任务恢复只在已有计划任务事实周围有限窗口内匹配“任务名 + 启动 + 绝对/环境变量路径”行，不凭空命名；IOC 过滤不会删除被任何技术 URL 或网络事实使用的主机；文档 URL 只按明确文件后缀改绑；外传事实必须在同一来源片段同时满足清单对象和发送动作。所有新增关系均标为 `derived` 并引用两端，不冒充报告逐字关系。
- 语义基线升级：最终输出快照现在直接从发布目录读取任务内容、创建方式、网络绑定、命令、注册表动作和 IOC；概念行为由最终来源事实与原生事件共同推导。APT-C-48 基线明确要求两个任务、10 分钟周期、COM 创建、诱饵 URL 不得作为 C2、BIOS 键只读、`cmd.exe /c` 只负责诱饵打开、发布平台域名不得进入 IOC，并覆盖外传、组件下载和两次自删除。
- 回归证明：报告导入/编译/模型/管线相关 190 个测试通过，Ground Truth 与模型相关 153 个测试通过；完整原生来源语义集成测试 16 个全部通过，耗时 79.52 秒；相关 Ruff 与 `git diff --check` 通过。
- 样本处置：当前 `apt-c48-run-01` 将保留并改名为 `apt-c48-run-01-internal-only-semantic-fail`，作为“内部硬门通过但来源语义不合格”的诊断证据，不覆盖、不删除，也不计入最终验收。
- 下一步：提交后从全新目录重新执行三次 APT-C-48 PDF 正式管线；每次都必须同时通过标准校验、内部生成后硬门、离线内部重评和独立来源语义基线，才计为一次成功。
- 提交：本次使用 `fix: 恢复跨页任务与报告网络语义`。

### 2026-08-28：工作包 8 第 19 轮——补齐中文条件自删除的独立评估语义

- 第一次新验收运行：旧诊断样本已无损改名为 `apt-c48-run-01-internal-only-semantic-fail`；新的 `apt-c48-run-01` 从原 PDF 独立运行并发布，得到 26 个来源事实、106 个物化事件、100% 内部覆盖率、100 分，MiniMax 共 4 次调用。离线内部重评仍为 100 分、零硬失败。
- 独立基线首次结果：基线只报告缺少 `conditional_self_delete`。检查最终产物发现来源事实已明确表达“检测到调试环境或虚拟机时触发自毁机制”，并且真实 `file_delete` 事件已进入 Ground Truth 和记录级标签；缺失来自评估器只识别“如果/若/一旦/if/when”，不是最终数据语义丢失。
- 通用修改：独立语义快照保留原有显式条件词，并新增受限中文句式识别——前件必须出现“检测到/发现/处于/存在”，随后在有限距离内出现“时/后”和“触发/自毁/删除”。普通“工作完成后删除自身”仍归类为收尾清理，不会因为单独一个“后”误判为条件自毁。
- 回归证明：新增同一快照同时含“检测到……时触发自毁”和“工作完成后删除自身”的正反组合用例；完整来源语义集成测试由 16 项增至 17 项并全部通过，耗时 78.92 秒；Ruff 与差异检查通过。
- 后续来源基线结果：修正评估器后，`run-01` 暴露出另一项真实缺失——第二个 `User_Feed_Synchronization` 任务没有进入最终事件。因此本轮输出仍不计为成功，继续按真实缺失修复，绝不因评估器修正直接判定通过。
- 提交：本次使用 `test: 识别中文条件触发的自删除`。

### 2026-08-28：工作包 8 第 20 轮——忽略跨页任务行中的可见 PDF 页码

- 真实缺失复现：修正条件句评估后，`apt-c48-run-01` 只生成了 `SCS-Update`；第二个 `User_Feed_Synchronization` 没有任务事件。最终产物同时含一个无内容、无周期、创建方式不明的引擎任务，表明早期“计划任务相关信息”概括被物化，但详细表格第二行没有被恢复。
- 原始文本结构：PDF 第 6 页末尾包含 `User_Feed_Synchronization 启动`，第 7 页开头包含其二进制路径；PDF 文字层还在页首插入可见页码 `7/8`。旧跨页任务正则能跨换行，却把这个页码当成“启动”和路径之间的非空技术内容，因此只匹配第一行。此前 TXT 回归没有模拟可见页码，形成测试盲区。
- 通用修改：在有限计划任务表窗口内，仅删除行首形如 `当前页/总页数` 的可见 PDF 页眉，再执行任务行、周期和创建方式解析。原始来源片段和引用不修改；任务名、路径、触发和 COM 仍必须逐字存在于来源片段，页码清理只影响结构识别视图。
- 回归升级：跨页任务夹具在第二个二进制前加入 `7/8`，继续断言两个精确任务实体、各自二进制、10 分钟周期、COM 创建方式及事实关系全部恢复；直接在真实 PDF 的相同窗口上运行解析器，也确认得到两行任务。
- 回归证明：报告导入、编译、模型、管线、质量和 Scenario 适配共 198 个测试全部通过；完整来源语义集成测试 17 个全部通过，耗时 79.54 秒；Ruff 与差异检查通过。
- 样本处置：当前 `apt-c48-run-01` 将改名保留为 `apt-c48-run-01-semantic-fail-page-header`，不覆盖、不删除、不计为成功。提交后使用全新 `apt-c48-run-01` 再次独立运行。
- 提交：本次使用 `fix: 忽略跨页任务中的 PDF 页码`。

### 2026-08-28：工作包 8 第 21 轮——稳定恢复诱饵下载、伪装和进程枚举语义

- 重跑结果：页码修复后再次从空目录生成 `apt-c48-run-01`。B 首次和唯一结构修复均未形成完整有效审核，按安全策略整份丢弃；A 账本经本地确定性层后仍完整发布。得到 13 个来源事实、95 个事件、100% 内部覆盖率、100 分，MiniMax 共 3 次。两项任务及其二进制、10 分钟周期和 COM 创建方式均已进入最终任务事件。
- 独立来源基线：内部离线重评仍为 100 分、零硬失败，但独立基线缺少 `masqueraded_executable`、`decoy_document_download` 和 `process_enumeration`，因此该输出仍不计成功。最终事实显示：A 把“只下载伪装 PDF”标成通用 `payload_download`；反分析事实已明确写“遍历进程列表”且物化大量进程访问，只是评估映射仅从 `host_recon` 识别枚举；第一页图标/后缀伪装本轮完全漏抽。
- 诱饵下载修正：当且仅当通用载荷下载事实明确包含下载动作和诱饵/伪装文档，且不含攻击组件、后续组件、payload 或 component 时，确定性改为 `decoy_document_download`；混合“伪装文档及后续攻击组件”继续保留通用载荷角色。文档后缀 URL 同时从所有非诱饵网络/下载事实解绑，避免复制到 C2 或后续组件下载。
- 伪装上下文恢复：若任一来源事实已经明确表达可执行文件伪装，则不增加内容；否则只在同一来源片段同时出现“可执行文件/PE”、图标/文件名/后缀和伪装/隐藏/诱导三组标记时，恢复一个 `source` 的 `other` 上下文事实。它不伪造主机事件、不进入可物化分母，但在最终 Ground Truth 中稳定保留报告明确的社会工程伪装语义。
- 进程枚举评估修正：独立快照从 `host_recon` 或 `anti_debug_anti_vm` 中识别明确的“遍历进程/进程列表”；反分析用途不会使实际枚举动作消失。伪装概念也可由来源 `other` 上下文推导，不再强迫其冒充执行行为。
- 安全边界：所有新增事实必须引用单一真实来源片段；文档角色重分类拒绝混合组件下载；伪装恢复必须三组标记同时满足并全报告至多补一项；没有给 MiniMax 增加调用，也没有放宽内部质量门或来源语义基线。
- 回归证明：新增可执行文件伪装来源恢复、纯文档下载正例、混合文档/组件下载反例，以及反分析进程枚举和 `other` 伪装语义映射测试。受影响单元测试 106 个全部通过；完整来源语义集成测试增至 18 个并全部通过，耗时 78.52 秒；Ruff 与差异检查通过。
- 样本处置：当前 `apt-c48-run-01` 将改名保留为 `apt-c48-run-01-semantic-fail-a-omissions`，不计为成功。提交后再次使用全新 `apt-c48-run-01`，验证没有有效 B 时也能一次通过完整来源基线。
- 提交：本次使用 `fix: 稳定恢复诱饵与伪装语义`。

### 2026-08-28：工作包 8 第 22 轮——发布页域名优先于模型技术错绑

- 重跑结果：诱饵/伪装修复后再次生成 `apt-c48-run-01`，A 两个分块完成，B 首次结构校验失败后一次修复成功；最终得到 20 个来源事实、105 个物化事件、100% 内部覆盖率和 100 分，MiniMax 共 4 次。两任务、诱饵下载、可执行文件伪装、进程枚举、外传和自删除均已通过独立快照检查。
- 唯一来源基线失败：报告 IOC 共 10 项，多出 `mp.weixin.qq.com`；期望为 3 个哈希、4 个 URL、1 个攻击域名和 1 个 IP，共 9 项。旧过滤器通常能删除该发布站域名，但本轮模型先把它错误连接到技术事实，过滤器把“存在技术关系”当作保留依据，覆盖了发布页上下文判断。
- 通用修改：从来源片段识别首页可见的“可选当前页/总页数 + 域名 + `/s` + 报告标题/分析报告”发布头。若该域名没有在其他片段以明确 IOC 上下文或去武器化形式独立出现，则把它标为仅发布上下文；在自动关系绑定前删除对应域名/URL 实体及模型错绑关系，并在最终报告 IOC 过滤中再次排除。
- 保留边界：不按固定域名写死；必须同时满足首页结构、`/s` 发布路径和报告标题标记。同域名若在另一来源片段以附录 IOC、攻击指标或 `[.]`/`(.)`/`{.}` 形式出现，则视为独立技术证据并保留。
- 回归升级：原有“发布域名删除、明确 C2 保留”用例新增一条模拟模型把发布实体连接到 C2 事实的错误关系；断言发布 IOC、实体和关系全部删除，而明确 C2 域名与技术关系仍保留。
- 回归证明：受影响的报告导入、编译、管线、质量和适配共 106 个测试全部通过；完整来源语义集成测试 18 个全部通过，耗时 76.32 秒；Ruff 与差异检查通过。
- 样本处置：当前 `apt-c48-run-01` 将改名保留为 `apt-c48-run-01-semantic-fail-publisher-binding`，不计成功。提交后再从空目录生成；目标仍是连续三次完整来源基线通过，而不是内部满分。
- 提交：本次使用 `fix: 删除发布域名的技术错绑`。

### 2026-08-28：工作包 8 第 23 轮——任务表定位与任务事实承载解耦

- 重跑结果：发布域名修复后再次生成 `apt-c48-run-01`，A/B 均一次完成，得到 12 个来源事实、103 个物化事件、100% 内部覆盖率、100 分，MiniMax 共 3 次；IOC 已精确恢复为 9 项。独立来源基线仍拒绝，缺少第二任务二进制 `%UserProfile%\\AppData\\Local\\Microsoft\\Feeds\\msfeedsync.exe`。
- 事实形态：本轮 A/B 把计划任务拆成两个事实：一个描述 COM 创建两个后续组件任务，另一个只描述当前时间开始、每 10 分钟执行。最终第一项精确任务绑定创建事实，纯周期事实被编译成一个无名、无二进制、无周期的引擎任务；第二条表格任务虽然可被正则解析，但没有唯一事实关系，因此没有进入最终事件。
- 深层根因：第 20 轮只修正了表内页码，却仍用“模型计划任务事实引用的片段位置”决定表格扫描窗口，并依赖通用同片段唯一关系规则绑定实体。模型引用位置和事实拆分一变，确定性恢复就失效；这是架构入口仍依赖模型锚点，不是提示词再跑一遍能稳定解决的问题。
- 全篇表定位：只要已验证账本存在计划任务行为，除模型引用位置外，还从全篇精确查找“任务名 + 动作/操作/路径”或英文等价表头，以该表头建立有限相邻窗口；仍要求每一任务行满足精确任务名、启动动作和绝对/环境变量二进制路径。可见 PDF 页码清理继续只作用于解析视图。
- 事实承载：表内任务优先绑定描述中点名该任务或二进制的唯一事实；否则绑定已经承载其他表内任务的唯一事实；再否则绑定唯一含“创建/建立/注册”的计划任务事实。这样一个明确的“创建两个任务”事实可以生成两个精确任务事件，而不会把二者错误当成两个独立攻击阶段。
- 上下文降级：在已有精确任务承载事实时，未绑定任务且只描述当前时间、周期、触发器或任务相关字符串的事实改为 `other` Ground Truth 上下文，保留来源和不确定性原因，不再生成隔离无名任务。真正包含创建动作或没有任何精确任务承载的事实不降级。
- 真实 PDF 直接验证：不调用 MiniMax，以真实 PDF 来源片段和“创建事实 + 周期事实”最小账本运行确定性丰富，稳定得到两个任务、两个关系、两条精确二进制、相同 10 分钟周期与 COM 创建方式；周期事实正确降为 `other`。
- 回归证明：跨页夹具改为模型创建事实只引用表格之前的片段，并增加独立周期事实；仍断言两任务全部绑定创建事实，周期事实降级。受影响单元测试 106 个全部通过；完整来源语义集成测试 18 个全部通过，耗时 78.82 秒；Ruff 与差异检查通过。
- 样本处置：当前 `apt-c48-run-01` 将改名保留为 `apt-c48-run-01-semantic-fail-task-anchor`，不计成功。提交后再次全新运行；这次任务恢复不再依赖模型是否引用表格所在页。
- 提交：本次使用 `fix: 解耦任务表与模型事实锚点`。

### 2026-08-28：工作包 8 第 24 轮——恢复误标的条件自删除并以事件证明 BIOS 查询

- 重跑结果：任务表解耦后再次生成 `apt-c48-run-01`，A/B 均一次完成，得到 14 个来源事实、93 个事件、92.3% 受控行为覆盖率、100 分，MiniMax 共 3 次。IOC 恰好 9 项；最终任务事件恰好两个，均有精确二进制、10 分钟周期和 COM 创建方式；不再有空任务。
- 独立来源基线：任务与 IOC 全部通过，只缺 `bios_registry_query` 和 `conditional_self_delete`。检查最终事件证明 BIOS 查询实际存在：`registry_query` 精确指向 `HKEY_LOCAL_MACHINE\\HARDWARE\\DESCRIPTION\\System\\SystemBiosVersion`；缺少标签只是评估器仅从事实描述识别 BIOS。条件自删除则确实没有物化：A 把“如果检测到调试环境或虚拟机则自毁”标为 `anti_debug_anti_vm`，没有检查目标，最终 Ground Truth-only；只有工作完成后的收尾删除产生事件。
- 条件后果重分类：对被标为反分析的事实，只有同时具备显式条件词或“检测到/发现/处于……时/则/后”、删除/自毁后果，且不含遍历/枚举/比对进程、查询/读取注册表等实际检查动作时，才确定性改为 `self_deletion`。编译器随后产生真实 `file_delete`，而不是只添加概念标签。
- 去重边界：若同一来源片段已有独立 `self_deletion` 事实，则不再重分类，继续由既有反分析上下文去重逻辑降为 `other`，避免同一条件后果生成两次删除。包含实际检查动作的复合事实也不整项改类，防止丢失反分析检测。
- BIOS 评估修正：独立语义快照既接受事实描述中的 BIOS/注册表，也直接检查带相同 fact ID 的最终 `registry_query` 事件是否精确以 `SystemBiosVersion` 结尾。只有真实查询事件存在才推导 BIOS 查询概念；注册表写入仍由禁止基线单独拒绝。
- 回归证明：新增“仅条件自毁、误标反分析”恢复用例；原有“同片段已有自删除则条件反分析降为 other”用例继续通过；语义映射新增实际 BIOS 查询事件断言。受影响单元测试增至 107 个并全部通过；完整来源语义集成测试 18 个全部通过，耗时 80.20 秒；Ruff 与差异检查通过。
- 样本处置：当前 `apt-c48-run-01` 将改名保留为 `apt-c48-run-01-semantic-fail-condition-role`，不计成功。提交后再次全新运行，验证两类自删除均有独立事件且 BIOS 查询由物理事件证明。
- 提交：本次使用 `fix: 恢复条件自删除的实际事件`。

### 2026-08-29：工作包 8 第 25 轮——隔离任务二进制与系统信息外传文件语义

- 正式运行结果：条件自删除修复后从空目录运行 `apt-c48-run-01`。A 两个分块完成，B 首次结构失败后使用一次修复，共 4 次模型调用；生成前质量门和 Scenario 校验通过，原生生成器在第 3 小时正确中止：一个连接事件把 `%UserProfile%\\AppData\\Local\\Microsoft\\Feeds\\msfeedsync.exe` 作为 `source_file`，但该文件此前没有创建。临时目录已清理，正式 `apt-c48-run-01` 未创建。
- 深层根因：模型把计划任务二进制 FILE 实体错误关联到 `c2_system_info_exfil`。旧编译器只要某个外传事实绑定唯一 FILE，就无差别写入 `source_file`；但 `c2_system_info_exfil` 的语义是用户名、进程列表和模块列表，不是文件上传。即使绕过原生时序检查，这条文件对象关系也不贴合报告。
- 账本层修正：收集精确计划任务 `binary` 属性对应的文件规范值；对于 C2、系统信息外传、DNS 等非下载网络事实，若其描述没有逐字点名该路径，则删除指向任务二进制 FILE 的模型关系。`payload_download`/`payload_download_execute` 仍可把任务二进制作为下载目标；明确在外传事实描述中点名路径的其他报告也不会被机械删除。
- 编译层防御：网络事件只在下载行为中把 FILE 当目标对象，只在通用 `data_exfiltration` 中把唯一 FILE 写为 `source_file`；`c2_system_info_exfil` 不再携带文件字段或文件对象 ID。这样错误关系即使漏过账本清理，也不会使主机时序或最终 Ground Truth 伪造文件上传。
- 回归证明：新增“任务二进制与系统信息外传错绑删除、下载关系保留”账本用例，以及“系统信息外传即使绑定 FILE 也不生成 `source_file`/对象 ID”编译器用例。受影响单元测试增至 109 个并全部通过；完整来源语义集成测试 18 个全部通过，耗时 79.99 秒；Ruff 与差异检查通过。
- 下一步：提交后直接使用仍不存在的 `apt-c48-run-01` 重跑；原生生成必须完整结束，然后再执行内部和来源双重离线验收。
- 提交：本次使用 `fix: 隔离任务二进制与外传文件`。

### 2026-08-29：工作包 8 第 26 轮——从来源片段恢复自删除的条件限定

- 重跑结果：任务二进制/外传隔离后再次从空目录生成 `apt-c48-run-01`，A/B 均一次完成，原生 8 小时生成和原子发布成功；得到 15 个来源事实、100 个物化事件、100% 内部覆盖率、100 分，MiniMax 共 3 次。内部离线重评通过；任务、IOC、BIOS、诱饵绑定和收尾自删除均通过来源基线。
- 唯一基线失败：缺少 `conditional_self_delete`。最终实际上已有两个独立 `file_delete`，但第一个来源事实被 A 概括为“样本使用 SetFileInformationByHandle 删除自身”，丢掉了原片段中的“如果处于调试环境或虚拟机则触发自毁”限定。因此物理事件存在，报告语义仍不完整，不能判成功。
- 深层根因：第 24 轮只处理“模型把条件后果错标为反分析”，仍依赖模型描述保留条件词；本轮模型标签已是 `self_deletion`，但摘要丢条件，规则无法识别。这再次说明恢复入口必须读来源片段，而不是依赖模型摘要措辞。
- 来源驱动恢复：扫描来源片段时必须同时满足显式条件（如果/若/一旦/if/when 或“检测到/发现/处于……时/则/后”）和自毁/删除自身语义；普通“如果发现感染建议删除文件”不满足“自身”目标，不会被误收。条件片段引用的既有 `self_deletion` 摘要若漏条件，则确定性补回“如果反分析条件成立”；误标的无检查动作事实继续改类；模型完全漏抽时新增稳定的 `source` 自删除事实。
- 去重与动作边界：同片段已有自删除事实时不再新增第二项；包含遍历/枚举/比对进程或查询/读取注册表的实际检查事实不整项改类。新增或修正事实都引用真实条件片段并记录不确定性原因，随后由编译器产生真实删除事件。
- 回归证明：新增“已有自删除事实但摘要遗漏条件”和“模型完全漏抽条件自删除”两类回归；既有误标反分析与同片段去重回归继续通过。受影响单元测试增至 111 个并全部通过；完整来源语义集成测试 18 个全部通过，耗时 80.34 秒；Ruff 与差异检查通过。
- 样本处置：当前 `apt-c48-run-01` 将改名保留为 `apt-c48-run-01-semantic-fail-condition-summary`，不计成功。提交后重新生成，验证来源条件限定在不同模型摘要下稳定保留。
- 提交：本次使用 `fix: 从来源恢复自删除条件`。

### 2026-08-29：工作包 8 第 27 轮——拆分条件自毁与收尾清理

- 连续验收进度：修复条件限定后，`apt-c48-run-01` 得到 16 条来源事实、95 个事件、4 次模型调用；`apt-c48-run-02` 得到 15 条来源事实、100 个事件、3 次模型调用。两轮均为内部覆盖率 100%、离线内部重评 100 分、独立来源语义基线通过，精确保留 9 项 IOC 和 2 项 COM/10 分钟计划任务。
- 第三轮失败：`apt-c48-run-03` 的 B 审核首次和定向修复均无效，系统正确丢弃整份 B，仅使用已验证 A 账本继续；最终仍以 19 条来源事实、104 个事件、100% 覆盖率和 100 分发布。独立基线发现缺少 `cleanup_self_delete`，因此不计成功，并保留为 `apt-c48-run-03-semantic-fail-cleanup-self-delete`。
- 深层根因：报告第 5 页明确说明“发现调试/虚拟机则自毁”，第 7 页另外说明“主要工作完成后删除自身清理痕迹”。两者都使用 `SetFileInformationByHandle`，A 将两个跨页片段合并为一个条件自删除事实。旧来源恢复能识别条件限定，却没有校验同一事实是否还吞并了不同触发条件的收尾删除。
- 提示词修正：A 升级为 `report-facts-v4`，B 升级为 `report-audit-v5`。两者都明确要求，条件自毁与工作完成后清理是两个原子行为；即使使用同一 API 或文件，也必须以两个 `self_deletion` 事实分别引用来源，B 发现合并时应使用 `SPLIT_FACT`。
- 来源级修正：确定性层从已验证片段中识别“工作/任务/执行完成或结束 + 删除自身/自毁 + 清理痕迹”。若模型事实同时引用条件与清理片段，先把清理片段从条件事实的来源引用中分离，再为清理片段恢复独立事实；已有清理事实时不重复创建。
- 安全边界：清理恢复必须同时满足执行完成/清理上下文和明确的自身删除动作；不因普通“删除文件”或单独“完成后”补行为。恢复事实使用真实来源片段和稳定 ID，不依赖 MiniMax 摘要措辞。
- 回归证明：新增“模型将两段自删除合并”和“模型已有收尾清理不重复”两类回归；报告导入单元测试 74 项通过，相关 Ruff 与差异检查通过。
- 下一步：本次提交后重新运行第 3 轮；由于发现了真实语义失败，连续成功计数必须从零重新开始。
- 提交：本次使用 `fix: 分离条件自毁与收尾清理`。

### 2026-08-29：工作包 8 第 28 轮——从已选 URL 闭合报告域名 IOC

- 新一组验收：基于提交 `e540526` 从零开始计数。`apt-c48-acceptance-01` 得到 21 条来源事实、103 个事件和 3 次模型调用，内部覆盖率/离线评分均为 100，独立来源语义基线通过，两类自删除均有最终行为。
- 第二轮失败：`apt-c48-acceptance-02` 的 B 首次响应经一次定向修复后有效，最终以 19 条来源事实、101 个事件、4 次调用、100% 覆盖率和 100 分发布。独立基线发现域名 IOC 为空，而 4 个 `panbaiclu.com` 技术 URL、IP 和 3 个哈希均存在。该轮不计成功，已保留为 `apt-c48-acceptance-02-semantic-fail-domain-closure`。
- 深层根因：本地 IOC 提取器会同时识别 URL 和其主机，但为避免把报告发布页/厂商引用当成攻击基础设施，旧合并阶段只确定性保留哈希和 IP，URL/域名完全依赖 A/B 语义选择。模型本轮选中了每个攻击 URL，却没有重复列出同一域名；旧质量门只校验已有 IOC 的保留率，因此分子/分母同时少一项仍得 100 分。
- 确定性闭包：只对已经被 A/B 选为报告 IOC 的 URL 解析主机；主机为合法域名且账本没有同值 DOMAIN 时，复用 URL 的真实来源片段生成稳定 DOMAIN IOC。这不会从所有页面链接盲目提取，不对 IP 字面量生成域名，已有域名不重复。
- 门禁升级：生成前质量门和已发布离线重评都检查“URL 域名闭包”。使用本轮真实失败产物重评时，新门禁明确返回 `passed=False` 和“缺少 panbaiclu.com”；类似缺失不再能通过内部门禁。
- 回归证明：新增已选去武器化 URL 闭合域名、IP URL 不误生域名、已发布 URL 无域名被拒绝三类测试。报告导入与质量单元测试 85 项通过；报告导入、编译、质量和完整来源语义测试共 124 项全部通过，耗时 79.76 秒；相关 Ruff 与差异检查通过。
- 下一步：提交后再次从零启动三轮连续真实 PDF 验收。
- 提交：本次使用 `fix: 闭合技术 URL 的报告域名`。

## 质量验收记录

### APT-C-48 真实 PDF 连续三轮验收

三轮均基于同一代码提交 `d41d9f8`，从原 PDF 重新读取文字层、独立调用 MiniMax，不复用模型响应或中间账本。

| 输出目录 | 模型调用 | 来源事实 | 故事线事件 | 内部覆盖率 | 离线内部分 | 独立来源基线 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| `apt-c48-final-01` | 4 | 19 | 104 | 100% | 100.00 | 通过 |
| `apt-c48-final-02` | 4 | 18 | 98 | 100% | 100.00 | 通过 |
| `apt-c48-final-03` | 4 | 17 | 97 | 100% | 100.00 | 通过 |

- 每轮最终都恰好保留 9 项 IOC：3 个哈希、4 个 URL、1 个域名、1 个 IP；不包含报告发布页域名。
- 每轮最终都恰好保留 `SCS-Update` 和 `User_Feed_Synchronization` 两个计划任务，二进制路径、10 分钟周期和 COM 创建方式均正确。
- 每轮都通过诱饵文档下载/打开、可执行文件伪装、用户执行、进程枚举、BIOS 注册表查询、主机清单外传、攻击组件下载、条件自毁和收尾清理等独立语义断言。
- 禁止项均通过：未虚构 PowerShell，`cmd.exe /c` 没有被误认为后续攻击主体，`Architecture.pdf` 没有被误绑 C2/攻击载荷，BIOS 动作未被写成注册表写入，没有无来源精确断言。
- 三轮 B 审核的首次响应都未通过本地校验，唯一定向修复也均无效；系统每次都丢弃整份 B，仅使用已验证 A 与本地确定性层仍连续三次通过。这证明最终质量不依赖多跑审核模型的随机补全。
- 三个目录分别约 8.25 MiB、8.07 MiB 和 8.59 MiB，均含标准 `scenario.yaml`、原生数据目录、`GROUND_TRUTH.*`、`GOLD_Label.json`、`RECORD_GROUND_TRUTH.jsonl`、附件产物和 `REPORT_FIDELITY.json`。

### TXT 边界用例

- “简短无 IOC”、“仅网络/C2”、“多任务 + 多文件 + 多 URL”三类虚构 TXT 全部通过完整离线主管线、原生 `GenerationEngine`、生成前/后质量门和独立语义断言；各自覆盖率与质量分均为 100%、100.00。
- 多任务用例额外验证两个 URL 不串绑文件、两个任务的 20/30 分钟周期和任务 XML 均正确。无 IOC 用例明确禁止凭空增加 IOC。
- 这三类使用已固定的脱敏 A/B 结构响应，目的是验证 TXT 抽取后的编译、生成和质量门不丢语义；不把它们冒充为真实 MiniMax 开放输入抽取质量证据。开放输入的真实模型波动由上述 PDF 三轮验收覆盖。

### 验收结论与边界

- 结论：当前实现已达到“TXT/可复制文字 PDF 输入后，单轮主管线直接产生较高来源贴合度 EvidenceForge 数据集”的当前验收目标。
- 可见性限制：报告明确但当前没有原生日志表达的上下文会保留在 Ground Truth；为产生可观测记录而建立的进程、用户、主机、时间和最小权限均明确标为引擎生成，不声称来自报告。
- 输入边界：只支持 `.txt` 和有可提取文字层的 `.pdf`；扫描版/纯图片 PDF 仍不支持 OCR。加密、空文本、超限文件会在模型调用前拒绝。
- 模型边界：A 是必需的来源事实阶段，无效、鉴权失败或网络失败时不发布产物；B 只是可选完整性顾问，它的不完整/无效结果整份丢弃；C 只处理本地无法机械完成的白名单仿真字段，不允许补外部 IOC 或新攻击行为。
- Web 接入边界：后续 Web 层只需将上传文件与唯一输出目录传给现有八阶段命令，展示最终 EvidenceForge 文件和质量结论。不应将抽取文本、MiniMax 原始响应或中间账本暴露给终端用户。
